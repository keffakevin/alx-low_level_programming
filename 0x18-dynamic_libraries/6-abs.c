#include "main.h"
/**
 * _abs - function that print alahabetic
 * @n:parameter description
 * Return:1 and return 0 otherwise
 */
int _abs(int n)
{
if (n < 0)
	n = -(n);
else
	n = (n);
return (n);
}
