#include"main.h"
/**
 * positive_or_negative - function that print postive or negative
 * @i:parameter description
 * Return:1 and return -1 otherwise
 */
void positive_or_negative(int i)
{
if (i < 0)
	printf("%d is negative", i);
else
{
printf("%d is Positive", i);
}
printf("\n");
}
